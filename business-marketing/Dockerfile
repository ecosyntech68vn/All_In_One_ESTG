FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
ENV PYTHONUNBUFFERED=1
ENV PORT=5000
EXPOSE 5000
# Default: chạy comay-bot. Đổi entry tùy module bạn deploy.
CMD ["python", "comay-bot/app.py"]
